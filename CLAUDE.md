# CORE Suits — 作業のときの決まり

## 崩してはいけない4つ

1. **店舗の分離。** `organizations` 以外のすべての読み書きに `organization_id` を通す。`Store` の口が引数で強制している。外さない
2. **顧客写真は公開しない。** `public/` にも公開バケットにも置かない。配るのは `/api/media/[id]` のみ
3. **AI を呼んだら必ず原価が残る。** 成功でも失敗でも `ai_jobs` に1行。上限確認は生成より必ず先
4. **見本が実データを覆い隠さない。** Supabase の設定があれば必ず Supabase。`getStore()` の最初の return を変えない

## 嘘をつかない

- 「AI が書いた」と言えるのは、実際にモデルが返したときだけ。落ちたら「相性ルールによる要約」と表示する
- 生成画像には必ず注意書きを添える（3画面すべて）
- 実額と見込みを足さない。母数が少ないときは率を出さない
- 未実装は `Coming Soon` と明示する。動かないボタンを置かない

## 変更したら

```bash
npm run check    # 型 + テスト67件 + 恒久検査6本
npm run build
```

恒久検査を足したら `package.json` の `check` にも足す（`check:guards` が忘れを見張る）。
**検査を書いたら、わざと壊して落ちることを必ず確認する。** 落ちない検査は緑でも嘘。

## 触るとき注意

- `src/lib/domain/` は外部依存なし。ここに `server-only` や DB を持ち込まない（テストできなくなる）
- ページから純粋関数を `export` しない（Next.js のビルドが落ちる）。`lib/domain/` へ置く
- このリポは日本語パス（`~/マイル/`）配下。`new URL(import.meta.url).pathname` は %エンコードされる。`fileURLToPath` を使う
